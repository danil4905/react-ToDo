import React from 'react';
import { Form } from '../components/Form';

const Home = (props) => {
    return (
        <React.Fragment>
            <Form notes={[]}/>
        </React.Fragment>
    )
}
export default Home;